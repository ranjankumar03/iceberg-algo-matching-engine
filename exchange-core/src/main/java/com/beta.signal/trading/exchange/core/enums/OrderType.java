package com.beta.signal.trading.exchange.core.enums;

/**
 * Enum representing the type of order in a trading system.
 * <p>
 * This enum is used to specify whether an order is a buy or sell order.
 * </p>
 */
public enum OrderType {

    BUY,
    SELL
}